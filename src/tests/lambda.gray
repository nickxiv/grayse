func f(x) {
    return lambda (y) { 
        x + y ;
    };
};

let a = f(3);
let b = f(11);

println("a(5) is ", (a(5)));      // should be 8
println("b(6) is ", (b(6)));      // should be 17
println("a(10) is ", (a(10)));    // should be 13
