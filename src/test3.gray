//Written by Nick Martin for CS403 - Programming Languages

//The purpose of test3 is to test functionality of variables starting with underline;

func doNothing() {
    return;
};

func square(num) {
    doNothing();
    RETURN (num * num);
};

func add(a, b) {
    RETURN (a + b);
};

i = square(2);
j = add(square(2), square(3));
